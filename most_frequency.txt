def most_frequent(string):
    d = dict()
    for key in string:
        if key not in d:
            d[key] = 1
        else:
            d[key] += 1
    return d
input_string= input('Please enter a string ')
print (most_frequent(input_string))